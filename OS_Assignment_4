Create a file named "data.txt" and write the following lines in it(write as it is).
Linux is open source.
In Linux everything is file
files have permissions.
files have inode no.
files have size.
there are several types of files
compose the commands fulfilled following requirement

==>

ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ touch data.txt

ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ cat > data.txt
Linux is open source.
In Linux everything is file
files have permissions.
files have inode no.
files have size.


i. count the no. of words ,characters and lines from above 2 file.(man wc)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ wc data.txt
  	5  25 144 data.txt

ii.list the lines having word "files" (man grep)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ grep "files" data.txt
	files have permissions.
	files have inode no.
	files have size.
	there are several types of files

iii.list the lines having word "file" (man grep)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ grep "file" data.txt
	In Linux everything is file
	files have permissions.
	files have inode no.
	files have size.
	there are several types of files


iv.list the lines which don't have word "files" (man grep)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ grep -v "files" data.txt
	Linux is open source.
	In Linux everything is file


v. list the lines having word "have" . (man grep)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ grep "have" data.txt
	files have permissions.
	files have inode no.
	files have size.


vi. list the lines starts with letter "f" (man grep)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ grep "^f" data.txt
	files have permissions.
	files have inode no.
	files have size.


vii.list the lines ends with "." (man grep)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ grep "\.$" data.txt
	Linux is open source.
	files have permissions.
	files have inode no.
	files have size.


viii. list only first two lines.(man head)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ head -n 2 data.txt
	Linux is open source.
	In Linux everything is file

ix. list only last three lines.(man tail)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ tail -n 3 data.txt
	files have inode no.
	files have size.
	there are several types of files

x.list line no.3,4 and 5 . (man head and tail)

	ssj@ssj-Lenovo-Legion-5-15IMH05:~/dir1$ head -n 5 data.txt | tail -n 3
	files have permissions.
	files have inode no.
	files have size.























